# RTD_LiveOps (Real-Time Dashboard)

This item is authored in the Fabric workspace UI. Create it in the DEV
workspace, then let workspace Git integration commit its definition files
into this folder (replacing this placeholder). fabric-cicd will then
promote it to test/prod like every other item.

Setup notes are in docs/architecture.md.
